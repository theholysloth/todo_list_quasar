export default function () {
  return {
    // ce fichier est comme une table d'une BD , il sera la source unique des données globales
    // une fois qu'on en a besoin on acede aux données grace à computed là ou on a besoin de ces données
      tasks:[]
  }
}
